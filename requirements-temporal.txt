# Additional requirements for Temporal Liveness Transformer
# These are in addition to your existing requirements.txt

# Core dependencies (already likely installed)
torch>=2.0.0          # PyTorch deep learning framework
torchvision>=0.15.0   # Vision utilities for PyTorch

# Optional but recommended
opencv-python>=4.5.0  # Computer vision operations (already in your stack)

# Development/Training only
tensorboard>=2.10.0   # Monitoring training progress
numpy>=1.20.0         # Numerical computing (already in your stack)

# For evaluation metrics (optional)
scikit-learn>=1.0.0   # Classification metrics

# For profiling/optimization (optional)
# torch-profiler is built-in with PyTorch 2.0+

# GPU optimization (optional, for faster inference)
# If using CUDA 11.8+, torch is built with CUDA support
# For CPU-only installation, use: torch>=2.0.0 with cpu variant
